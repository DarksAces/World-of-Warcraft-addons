# Volumizer

**Descripción:**
Control de volumen preciso mediante comandos.

**Características:**
- Usa el comando `/vol <0-100>` para establecer el volumen maestro del juego a un porcentaje específico.

**Instalación:**
Copia la carpeta `Volumizer` a tu carpeta `Interface\AddOns`.
